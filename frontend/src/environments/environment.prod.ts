export const environment = {
  production: true,
  // Set this to your deployed backend's public URL (e.g. Railway),
  // with no trailing slash.
  apiBaseUrl: 'https://projectlens-znr9.onrender.com'
};
