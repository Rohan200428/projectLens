export const environment = {
  production: true,
  // Set this to your deployed backend's public URL (e.g. Railway),
  // with no trailing slash.
  apiBaseUrl: 'https://projectlens-production-2e97.up.railway.app'
};
