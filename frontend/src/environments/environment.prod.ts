export const environment = {
  production: true,
  // Set this to your deployed backend's public URL (e.g. Railway),
  // with no trailing slash.
  apiBaseUrl: 'https://REPLACE-WITH-YOUR-RAILWAY-BACKEND-URL'
};
